# Lift & Lean 環境変数テンプレート
# このファイルをコピーして .env を作る。.env は絶対にcommitしない。
#   cp .env.example .env

# ============================================================
# OpenAI（既存機能：食事推定・画像解析・AI目標提案）
# ============================================================
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# モデル指定（未設定時はコード内の既定値を使用）
# ESTIMATE_MEAL_SEARCH_MODEL=gpt-5.6-terra
# ESTIMATE_MEAL_SEARCH_FALLBACK_MODEL=gpt-4o
# SUGGEST_GOALS_MODEL=gpt-5.6-sol
# SUGGEST_GOALS_FALLBACK_MODEL=gpt-4.1

# 音声・文字の指示を操作へ振り分けるモデル（既定 gpt-4o-mini）。
# 1つの指示につき1回だけ呼ぶ。新しい鍵は要らない（OPENAI_API_KEY を使う）
# COMMAND_MODEL=gpt-4o-mini

# ============================================================
# Web Push 通知（既存機能）
# ============================================================
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Vercel Cron の認証用シークレット（未設定なら検証をスキップする実装）
CRON_SECRET=

# ============================================================
# Appwrite Cloud（新規：DB / Auth）
# ============================================================
# Appwrite Console > プロジェクト設定 から取得する。
# **エンドポイントはリージョンごとに違う。** Singaporeなら sgp.cloud.appwrite.io。
# Console のプロジェクト設定に表示されている API Endpoint をそのまま使うこと。
APPWRITE_ENDPOINT=https://sgp.cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=

# API key はサーバー専用。admin権限で全ての権限チェックをバイパスするため、
# フロントエンドへ出したり commit したりしない。
#
# ■ 本番稼働用（Vercelの環境変数に入れる）: rows.write と sessions.write
#     - rows.write     … 行の書き込み
#     - sessions.write … ログイン時のセッション発行
#       **APIキー無しで createEmailPasswordSession を呼ぶと secret が空で返る**
#       （Appwriteはブラウザ向けにCookieで返す設計のため）。空のsecretでは
#       ログイン状態を保てないので、セッション発行にはAPIキーが要る。
#   読み取り（rows.read）と users.* は不要のまま。
#   **このキーが漏れても利用者のデータを読み出すことはできない**性質は維持している。
#
# ■ スキーマ作成用（npm run db:setup のときだけ。実行後は削除する）:
#   databases.read / databases.write / tables.read / tables.write
#   / columns.read / columns.write / indexes.read / indexes.write
#   ※ スキーマは反映済みなので、変更するときだけ一時的に発行する
APPWRITE_API_KEY=

# データベースID。npm run db:setup がこのIDで作成する（未設定なら liftandlean）
APPWRITE_DATABASE_ID=liftandlean

# パスワード再設定メールのリンク先に使う、このアプリの公開URL。
# **リクエストのHostヘッダは使わない**（差し替えられるとリンクの宛先を乗っ取られるため）。
# Vercel上は未設定でも VERCEL_PROJECT_PRODUCTION_URL から自動で決まる。
# ここに設定するホストは Appwrite Console の Platforms にも登録が必要。
APP_PUBLIC_URL=

# 実Appwriteに対する統合テストを動かすときだけ 1 にする。
# 使い捨てユーザーを作って消すため、通常は未設定のままにしておく。
APPWRITE_ALLOW_INTEGRATION_TESTS=

# フロントエンドは Appwrite へ直接アクセスしない（必ず /api/v1 を経由する）ため、
# ブラウザ側に Appwrite の設定値は不要。

# ============================================================
# MCP / OAuth（新規：ChatGPT連携）
# ============================================================
# このMCPサーバー自身の公開URL。アクセストークンの audience 検証に使う。
# 例: https://lift-and-lean.vercel.app/api/mcp
MCP_RESOURCE_URL=

# データの読み込み元を切り替える安全弁。
# local = 従来通りlocalStorageを source of truth にする（移行前・rollback時）
# db    = Appwriteを source of truth にする（移行検証が通った後）
VITE_DATA_SOURCE=local
